<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head'); %>
</head>
<body>
    <div class = "formulari">
        <header >
            <%- include('partials/header'); %>
        </header>
        <h1>Lista de Productos</h1>
        <ul>
            <% products.forEach(products => { %>
                <li><%= products.id %>- <%= products.nom %></li>
            <% }); %>
        </ul>
    </div>
</body>

</html>
