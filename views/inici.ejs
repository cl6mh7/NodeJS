<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head'); %>
</head>
<body>
    <div class = "formulari">
        <header >
            <%- include('partials/header'); %>
        </header>
        <h1>Lista de Productos</h1>
        <form action="edit.ejs" method="get">
        <ul>
            <% products.forEach(products => { %>
                <li><a href="/edit?id=<%= products.id %>"><%= products.id %>- <%= products.nom %></a></li>
            <% }); %>
        </ul>
    </form>
    </div>
</body>

</html>
